<%= form_for @backend_car_model, :as => :backend_car_model, :url => locals[:path] do |f| %>
  <% if @backend_car_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@backend_car_model.errors.count, "error") %> prohibited this backend_car_model from being saved:</h2>

      <ul>
      <% @backend_car_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
